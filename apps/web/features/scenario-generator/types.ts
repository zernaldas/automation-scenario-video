export type FormState = {
  prompt: string;
  scenarioType: string;
  platform: string;
};

export type ResultRow = {
  title: string;
  status: string;
  prompt: string;
  scenarioType: string;
  platform: string;
};

export type SelectOption = {
  label: string;
  value: string;
};
